# Database Configuration
DB_USER=ahmed
DB_PASSWORD=0502747598
DB_HOST=localhost
DB_PORT=5432
DB_NAME=compass

# Database Connection String
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Test Environment Configuration
TESTING=true

# JWT Configuration
JWT_SECRET_KEY=a82552a2c8133eddce94cc781f716cdcb911d065528783a8a75256aff6731886
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application Configuration
APP_NAME=COMPASS
APP_VERSION=1.0.0
DEBUG=True
ENVIRONMENT=development

# Database Initialization Settings
# Initialization mode: dev, prod, or test
DB_INIT_MODE=dev

# Create admin user during initialization (true/false)
CREATE_ADMIN=true

# Optional: Predefined admin credentials (for development ONLY)
# IMPORTANT: Use strong, unique credentials in production
ADMIN_EMAIL=admin@aiwa.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=aiwa_admin_2024!

# Database Initialization Behavior
# Determines whether to drop and recreate tables
DB_DROP_TABLES=true

# API Configuration
API_V1_PREFIX=/api/v1
CORS_ORIGINS=["http://localhost:3000"]

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT="%(asctime)s - %(name)s - %(levelname)s - %(message)s"

# Python Path
PYTHONPATH=/c:/Users/ahmed/OneDrive/Desktop/COMPASS/Backend

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}
